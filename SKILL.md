---
name: "ordinote-organizer"
description: "通用笔记整理相关技能，包括：初始化并整理笔记（OCR结果）、向已有笔记中增添内容、联网搜索补充完善笔记中的略写部分、将笔记规范化整理、整理笔记文件夹结构、提取PDF/PPT/图片内容并整理成文、将笔记批量导出为 PDF 等；用户说有关普通的笔记整理时，使用该技能；注意实验类型的笔记不能使用该技能！"
---

# 通用笔记整理助手

这个技能帮助用户处理各类笔记相关的事项，包括整理、添加、补充完善和格式化

## 触发条件/使用场景

当用户需要：

- 整理一个或多个原始markdown笔记文档（通常是OCR识别结果）
- 要求整理笔记（非实验笔记）
- 要求将笔记规范化整理 / 生成整理版
- 要求向已有笔记中添加新的内容
- 要求补充完善笔记（补全笔记中略写、缩写的部分）
- 要求整理某个文件夹的结构（将笔记归类、移动到合理的文件夹中）
- 要求将 PDF/PPT/图片 的内容提取或整理出来（形成整理文档）
- 要求把笔记导出为 PDF（单个文件或整个目录批量导出）

**注意**：如果发现笔记内容是实验记录相关的内容，请使用 `expnote-organizer` 技能！

---

## 功能与引用指引

本 Skill 包含七个核心功能模块，分别定义在 `references/` 目录下的独立文件中。请根据用户的具体需求，查阅对应文件并按其规范执行。

### 1. 初始化并整理笔记

适用于：用户要求从零整理原始笔记文字稿（OCR 识别结果或录音转文字内容），生成规范的笔记文档。

请查阅 `references/ordinote-wash.md`，其中详细定义了：

- 输入来源说明（OCR 识别结果 / 录音转文字内容，各自的典型错误类型）
- 输出文件命名规则（`笔记整理-{今日日期}.md`，YYMMDD 格式）
- 错别字修正原则与方法（仅修正明显错误，保持原文意思不变）
- 公式的 LaTeX 转写规则（`$...$` / `$$...$$`，无法确定原意的保留原样并标注）
- 输出示例
- 内容完整性等注意事项

### 2. 向已有笔记中继续增添内容

适用于：用户要求将新的笔记（子笔记）合并到已有整理好的笔记（母笔记）中。

请查阅 `references/ordinote-add.md`，其中详细定义了：

- 母笔记与子笔记的归类规则（按内容匹配到对应母笔记）
- 复制与格式调整要求（复制子笔记内容并根据母笔记格式调整）
- 用户汇报义务（操作完成后需向用户详细说明修改内容）

### 3. 补充完善笔记（联网搜索）

适用于：用户要求对指定笔记进行补充完善，即识别笔记中所有只是略写、缩写、一笔带过的部分，联网搜索后补全，并用 `<mark>` 高亮标出。

请查阅 `references/ordinote-enrich.md`，其中详细定义了：

- 略写部分的识别标准（缩写、未展开的概念、省略内容、乱码公式等）
- 联网搜索与补充的处理流程
- `<mark>` 高亮规范与示例
- 公式的 LaTeX 转写规范（转写结果 `<mark>` 包裹，原公式保留原样）
- 原有内容零改动等注意事项

### 4. 笔记规范化整理（整理版）

适用于：用户要求将已有笔记重新组织为结构清晰、逻辑完整的整理版笔记（类似实验笔记的 protocol 格式化，但面向通用笔记，格式自适应）。

请查阅 `references/ordinote-standardize.md`，其中详细定义了：

- 输出文件命名规则（`{原文件名称}-整理版.md`）
- 格式确定规则（有以往整理好的笔记则参考其格式；没有或用户没说，则按笔记逻辑内容自行组织）
- 通用模板、分类标准与分层要求、板块合并规则与内容处理约束（不得删改已有内容，仅调整顺序和逻辑）

### 5. 笔记文件夹结构整理（organize）

适用于：用户要求整理某个文件夹下的笔记结构（归类、建立子文件夹、移动文件）。

请查阅 `references/ordinote-organize.md`，其中详细定义了：

- 两阶段工作流（先出方案，用户确认后才移动文件）
- 文件内容判定流程（能根据文件名判断的尽量不读取文件）
- 整理方案的三大原则（逻辑归属、聚类、合理的文件夹嵌套）
- 只移动不删改等安全约束

### 6. PDF/PPT/图片 内容提取（extract）

适用于：用户要求将 PDF/PPT/图片 的内容提取、整理出来（如"把这个 PPT 整理成文档""提取这个 PDF 的内容""把这张图片里的文字提取出来"）。

请查阅 `references/ordinote-extract.md`，其中详细定义了：

- 提取脚本调用（先本地脚本，失败或效果差时改用 MinerU API）；整理环节交由 `ordinote-wash` 完成
- 提取脚本说明（`scripts/` 下的本地脚本与 MinerU API 两种方式；均已实现）
- MinerU 支持的输入类型（PDF/PPT/DOC 及图片 png/jpg/jpeg/jp2/webp/gif/bmp；可直接上传图片做 OCR 提取文字）
- MinerU 支持一次上传多个文件批量提取（PDF 与图片可混用，每批 ≤ 50 个、官方单批上限 200 个，各文件分别输出）
- MinerU Token 配置（用 PowerShell 把 Token 写入用户环境变量 `MINERU_API_TOKEN`；脚本不读磁盘 Token 文件，未配置时会给出配置指引）
- 提取失败处理（无法提取或结果为空时必须如实告知，不得编造）
- 提取文件命名规则（`{原文件名}-提取.md`）与不删除原始提取文件等注意事项

### 7. 笔记批量导出 PDF（export）

适用于：用户要求把笔记导出为 PDF，或要求把某个目录下的笔记批量导出为 PDF。

请查阅 `references/ordinote-export.md`，其中详细定义了：

- 导出脚本（`scripts/md2pdf.py`；版式复用本机 MPE 扩展自带的样式与 mermaid 资源，观感与 MPE 预览一致；源笔记只读）
- 前置条件（用户需自行在本地类 VS Code 客户端安装 Markdown Preview Enhanced 扩展；本机需有 Chrome/Edge；不下载任何东西）
- 常用选项（`--out`、`--theme`、`--paper`、`--margin`、`--wait`、`--skip-existing`、`--exclude`）
- 渲染情况与降级行为（公式在解析前抽取并由 KaTeX 渲染，支持 `array` 等多行环境；`tikz` 代码块不渲染，需向用户说明；代码块无语法高亮；公式引擎缺失时以原始 LaTeX 呈现；未装扩展或浏览器时报错退出）
- 失败处理要求（如实告知，不得声称已完成导出）

---

## 文字输出风格全局约束

**修改或写入新内容**时（不写入和修改时，要求遵循各个功能模块的规范），要求：

<important>

- 禁止使用以下句式或类似的句式：
  - `一句话/一句话总结`
  - `先说结论`
  - `不是……而是`
- 禁止过度口语化，禁止反复累赘
- 减少第二人称（如“你”、“你的”、“你的”等）的使用
- 用语清晰，非必要不使用比喻
- 非必要不使用破折号 `——`，需要使用破折号的地方考虑能否使用别的符号代替
- 不是引用的部分不要使用引用代码块 `>`

</important>

